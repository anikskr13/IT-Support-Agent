# Optional: Set backend API base URL.
# If left unset or blank, the frontend uses relative API paths (ideal for unified deployment on Render)
# In development with Vite, defaults to http://localhost:8000
VITE_API_URL=
